# This is to try run TWO ngrok.yml files

# ngrok start --all --config=C:\josh\Voice-2.0\app\ngrok.yml

# authtoken: (can add authtoken but i dont want it in github)

version: 2

tunnels:
  twiml-server:
    addr: 3000
    proto: http

# this server needs to be configured in twilio (webhook)
  websocket-server:
    addr: 8080
    proto: http


